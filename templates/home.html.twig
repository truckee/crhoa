{# templates/home.html.twig #}
{% extends 'base.html.twig' %}
{% block body %}
<body {{ include('style.html.twig') }}
    <div class="container">
        {{ include('nav.html.twig') }}
        <div class="row mt-5">
            <div class="col-2"></div>
            <div class="col-8 text-center text-white"><h1>Castle Rock Homeowners Association</h1></div>
        </div>
    </div>
{% endblock %}
